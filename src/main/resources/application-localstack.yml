hmpps.sqs:
  provider: localstack
  queues:
    migrationvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationactivities:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationallocations:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationappointments:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationadjudications:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationincidents:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationcsip:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationlocations:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
      errorVisibilityTimeout: 0
    migrationcasenotes:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationalerts:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
      errorVisibilityTimeout: 0
    migrationprisonperson:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationcourtsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    audit:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventincidents:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventcsip:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventlocations:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventcasenotes:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventalerts:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventcourtsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventprisonperson:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
