hmpps.sqs:
  provider: localstack
  queues:
    migrationvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationincentives:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    audit:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
