hmpps.sqs:
  provider: localstack
  queues:
    migration:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    audit:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    event:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
