hmpps.sqs:
  provider: localstack
  queues:
    migrationvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationactivities:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationallocations:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationappointments:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationadjudications:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationincidents:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationlocations:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    audit:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventincidents:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventlocations:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventalerts:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
