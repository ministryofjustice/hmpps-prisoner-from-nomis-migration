hmpps.sqs:
  provider: localstack
  queues:
    migrationvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationincentives:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    migrationsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    audit:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    event:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventvisits:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventincentives:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
    eventsentencing:
      queueName: ${random.uuid}
      dlqName: ${random.uuid}
