server:
  shutdown: immediate

api.base.url:
  nomis: http://localhost:8082
  oauth: http://localhost:8090/auth
  # Set to ourselves until new service is ready
  visits: http://localhost:8083
  visit.mapping: http://localhost:8084
  sentencing: http://localhost:8085
  mapping: http://localhost:8083
  incentives: http://localhost:8083

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/migration?sslmode=prefer
    username: migration
    password: migration
    properties:
      sslMode: DISABLE
      sslRootCert:
  flyway:
    url: jdbc:postgresql://localhost:5432/migration?sslmode=prefer
    user: migration
    password: migration
